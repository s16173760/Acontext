apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aconext.fullname" . }}-test-connection"
  labels:
    {{- include "aconext.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aconext.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
