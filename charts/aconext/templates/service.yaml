apiVersion: v1
kind: Service
metadata:
  name: {{ include "aconext.fullname" . }}
  labels:
    {{- include "aconext.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aconext.selectorLabels" . | nindent 4 }}
